# PPR-KODEX (영구 규칙 · 목표)

## 1. VP1 경성 목표 (Hard Goal)
전체 라인 하나의 통합 제품을 낸다: **전체 조립 STEP 1개 + 편집 가능한 원본 CAD + 대응 Isaac 씬 + BOM**, 체인 전체
`호퍼 → S1 → 슈트 → S2 → 버퍼 → 수평 압출기 → 냉각 → 풀러 → 스풀`, 프레임/구동계/전기/가드/정비성 포함.

## 2. 고정 조건 (변경 금지, 합의 필요 시에만 사용자 승인으로 변경)
- **S1**: 2축 역회전 커터 스택.
- **S2**: 편심 궤도 + 저속 역회전 사이클로이드 착안 분쇄기. **실동력 경로 존재, 1-DOF, 제2모터 없음.**
- **구동**: 단일 M1(감속 + 동력 분할로 S1/S2 공유) + 별도 M2. **모터 미선정(UNSELECTED).**
- **전기**: 보유 PSU 24V 33A / 명판 800W (240x120x65). 500W는 소프트 운전 목표이며, 강제 상한은 전류 정격 기준 792W다.
- **본체**: 700x420x520mm 상한.
- **예산**: 추가비 soft limit 100,000 KRW. **미견적 != 0원** — 견적 없는 항목은 비용 미확정으로 취급.
- **소재**: PLA/PET/TPU. 퍼지(purge)와 인쇄 폐기물(printed waste)을 구분한다.

## 3. 방법론
- 후보군 전체 생성 → **실제 기하로 구동/유동/간섭 시험** → 결함 수정 → 재생성. 부분 PASS로 전체를 덮지 않는다.
- 시뮬레이션은 **결함 발견 도구**다. 보정되지 않은 근사는 성능/안전 검증으로 승격되지 않는다.
- **C2.3 트라이볼로미터/크라운/조인트 확장은 중단(STOPPED)**. 기존 계측 코드는 재사용 가능하나 신규 확장하지 않는다.

## 4. 검증 경계
- 유효한 검증: STEP 재가져오기(topology/volume), CAD↔BOM 대응, 실제 메커니즘 운동, 소재 경로 추적.
- **실행되지 않은 것 = 검증되지 않은 것(unverified)**. 디지털 PASS는 물리 해제 승인이 아니다.
- 구매/가공/통전/메인 병합은 사용자 승인 전 **HOLD**.

## 5. Git 경계
- 원본 dirty worktree `/home/monad/develop/PPR` 와 보존 브랜치는 **건드리지 않는다** (reset/clean/stash/일괄커밋 금지).
- 작업은 dev worktree에서 진행한다. **이 dev 브랜치에서의 의미 있는 변경은 커밋/푸시하여 PR #5를 갱신하는 것이
  KODEX 목표에 따라 허용된다.** HOLD는 메인 병합과 물리 해제(구매/가공/통전)에만 적용된다.
- 문서는 영구 규칙(KODEX)과 현재 상태(STATUS)만 유지한다. 위기 시 이 문서가 최상위 참조다.

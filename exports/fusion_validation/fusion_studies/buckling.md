# Buckling

LC10 frame에 실제 table anchor constraint와 peak frame reaction을 적용한다. 최소 5개 eigenmode와 factor를 내보내며 첫 factor≥2.0을 가상 설계 screen으로 한다. bonded profile joint는 볼트 미끄럼을 모델링하지 않으므로 물리 체결 검사를 대체하지 않는다.

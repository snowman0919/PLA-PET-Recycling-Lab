# Modal frequencies

LC10 frame과 LC01 shredder support에서 최소 10개 mode를 추출한다. 실제 조립 질량이 없는 donor envelope는 manifest mass override가 없으므로 임의 질량을 만들지 않는다. 운전 자극(shaft/cutter RPM 및 전원 주파수 harmonics)과 각 mode의 separation margin을 보고하며 최소 20% 분리를 합격으로 한다.
